# Updated-calibration gallery (pending rerun)

Populate this directory only after the rerun is accepted. The intended review
set is:

- round 0, 1, and (if run) 2 all-SPW mosaics;
- phase and amplitude views of every candidate gain table;
- the corrected eight-second spectrum;
- before/after 10–12 GHz tutorial summaries; and
- before/after individual-SPW, color-coded EOVSA contours over AIA 171.

Use `scripts/publish_run_gallery.py`; then inspect every copied PNG before
committing it. Measurement Sets, CASA tables, FITS cubes, and full `runs/`
directories remain local and must not be uploaded.
